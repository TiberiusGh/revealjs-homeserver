services:
  revealjs:
    image: ghcr.io/tiberiusgh/revealjs-homeserver:latest
    container_name: revealjs-server
    restart: unless-stopped
    ports:
      - '8085:8080'
    volumes:
      - ./slides:/app/slides:ro
